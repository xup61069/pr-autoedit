# 換行字元怎麼存。
#
# 沒有這個檔的話,每次 commit 都會噴一整片
# 「LF will be replaced by CRLF」的警告,把真正該看的訊息淹掉。
#
# 規則:版本庫裡一律存 LF,checkout 到 Windows 時交給 Git 處理。
# 但有兩種檔案例外,而且是踩過坑的:
#   .bat  中文版 cmd 對換行很敏感,而且這些檔必須是純 ASCII
#   .ps1  PowerShell 5.1 認 CRLF + UTF-8 BOM;換行不對會出怪事
# 兩者都強制 CRLF,不讓 Git 自作主張。

* text=auto eol=lf

# --- Windows 原生腳本:一定要 CRLF ---
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
*.reg text eol=crlf

# --- 二進位檔:不要碰 ---
*.png binary
*.jpg binary
*.ico binary
*.zip binary
*.vst3 binary
*.bin binary
*.mp4 binary
*.wav binary

# --- 不要打包進 Release 下載檔(git archive 會照 export-ignore 略過)---
#
# 發佈用的 zip 是給「只想裝來用」的人,不需要維護者/發佈/測試那些檔。
# 這裡標了 export-ignore 的,git archive 就不會收進去(版本庫裡照樣留著)。
# 注意:.bat 那幾個保留的是使用者要用的(安裝/移除);只排掉發佈那支。
.gitattributes    export-ignore
.gitignore        export-ignore
README.md         export-ignore
SETUP.md          export-ignore
PUBLISH.md        export-ignore
AGENTS.md         export-ignore
CONTRIBUTING.md   export-ignore
publish_release.ps1 export-ignore
發佈Release.bat   export-ignore
tests/            export-ignore
